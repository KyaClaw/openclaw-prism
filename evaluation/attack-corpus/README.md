# Attack Corpus

Put attack cases here as JSON files.

Recommended subdirectories:

- `direct-injection/`
- `indirect-injection/`
- `tool-abuse/`
- `exfiltration/`
- `file-tampering/`

The benchmark harness recursively loads `.json` files under this directory.
